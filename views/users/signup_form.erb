  
<%
val = {}
use_default_vals = !$prod
if use_default_vals
  val = {
    name: 'Some value',
    num: rand(1000000).to_i
  }
end
%>

<% if cu %>
<%  current_user = cu %>
<%   else %>
<%  current_user = {} %>
<% end %>


<div class='page1' style='background-color: #dbe9d8'>
  <p>
    <% if locals[:update_user] %>
    <h1 style="font-family: verdana;">Update Profile</h1>
    <% else %> 
    <h1 style="font-family: verdana;">Sign Up</h1>
    <% end %> 
  </p>

    <% if locals[:update_user] %>
    <form class='form-group' action='/update_user' method='post'>
    <% else %>
    <form class='form-group' action='/create_user' method='post'>
      <% end %>

      <p>
        <div class='input-group'>
          <label class='labelz'>Name</label>
          <input type="text" name='name' class='form-control' placeholder="Name" required value='<%=current_user["name"] %>'>
        </div>
      </p>

      <label>Phone</label>
      <img src="/img/il.png">

      <% if locals[:update_user] %>
        <%= current_user["phone"]%>
      <% else %>

      <div class='input-group'>
        <input type="text" name='phone' class="form-control bfh-phone placeholder="(054) 123-4567" data-format="972 (ddd) ddd-dddd" required >
      </div>
      <% end %>

      <p>
        <div class="input-group">
          <label>Profession</label>
          <select class="form-control" name='profession' required value='<%=current_user["profession"] %>'>
            <% PROFESSIONS.each do |prof| %>
            <option value="<%=prof%>"><%=prof%></option>
            <% end %>
          </select>
        </div>
      </p>

      <% use_address = false %>
      <% if use_address %>
      <p>
        <div class='input-group'>
          <label>Address</label>
          <input type="text" name='address' class='form-control' placeholder="Address" required value='<%=current_user["address"] %>'>
        </div>
      </p>
      <% end %>
      <p>
        <div class="input-group">
          <label>City</label>
          <select class="form-control" name='city'>
          <% CITIES.each do |prof| %>
            <option value="<%=prof%>"><%=prof%></option>
            <% end %>
          </select>
        </div>
      </p>

      <p>
        <div class='input-group'>
          <label>Description</label>
          <textarea rows="9" cols="20" name='description' class='form-control' placeholder="Enter your text here"  required ><%=current_user["description"] %></textarea>
        </div>
      </p>

      <div class="input-group">
        <label>Treatments</label>
        <div class="checkbox">
          <label><input type="checkbox" value="Manicure" name="treatments[]"   <%= "checked" if cu && "Manicure".in?(current_user["treatments"]) rescue nil %>>Manicure</label>
        </div>
        <div class="checkbox">
          <label><input type="checkbox" value="Pedicure" name="treatments[]"  <%= "checked" if cu && "Pedicure".in?(current_user["treatments"])rescue nil %>>Pedicure</label>
        </div>
        <div class="checkbox">
          <label><input type="checkbox" value="Make up" name="treatments[]" <%= "checked" if cu && "Make up".in?(current_user["treatments"]) rescue nil%>>Make up</label>
        </div>
      </div>

      <div class="checkbox">
        <label><input name="home_visits" type="checkbox" value="true" style = "width: 15px; height:15px;text-align:bottom; vertical-align:bottom;" <%= "checked" if current_user["home_visits"] %> >Home Visits</label>
      </div>

      <% if locals[:update_user] %>
      <button class='btn btn-lg btn-raised btn-custom-darken' type='submit'>Update</button>
      <% else %> 
      <button class='btn btn-lg btn-raised btn-custom-darken' type='submit'>Sign up &nbsp; →</button>
      <% end %> 
<!--     </p> -->

  </form>

</div>
